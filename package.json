{
  "name": "is-my-openai-key-expired",
  "version": "1.0.0",
  "description": "A minimalistic service to check if your OpenAI API key is expired",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

